#include <stdio.h>
#include <time.h>
#include <math.h>
#include <stdlib.h>
void main(){
	double m;
	int i;
	for(i =0 ;i<10; i++){
		m = rand();
		printf("time is %lf\n", m);
	}
	for(i =0 ;i<10; i++){
		m = rand();
		printf("time is %lf\n", m);
	}
printf("%lf\n",log(-1));
}
